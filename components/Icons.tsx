import React from 'react';
import { MapPin, X, PenTool, BookOpen, Navigation } from 'lucide-react';

export const Icons = {
  Pin: MapPin,
  Close: X,
  Write: PenTool,
  Read: BookOpen,
  Locate: Navigation
};
